


## 1. 图号整体格式
图号采用固定前缀 + 分隔符 + 层级码的结构：

[固定前缀] + "-" + [层级码]

text

- **固定前缀**：例如 `03S01201`，代表产品型号或项目代号，同一产品下所有零件/组件共用此前缀。
- **分隔符**：短横线 `-`
- **层级码**：表达父子关系和层级顺序的字符串（规则见下）。
**示例**：`03S01201-10`
---
## 2. 层级码的编码规则
层级码有两种扩展方式：**追加法**（线性扩展）和 **分叉法**（并列分支）。
### 2.1 追加法（用于组件层级深化的线性扩展）
- 在父级**层级码末尾**直接添加两位数字（`01` ~ `99`）。
- 结果仍然是一个**纯数字字符串**（不含短横线）。
- 适用场景：单一功能模块下的顺序展开。
**示例**：

父级图号：03S01201-10 (层级码 = "10")  
子组件1： 03S01201-1001 (层级码 = "1001")  
子组件2： 03S01201-100101 (层级码 = "100101")

text

### 2.2 分叉法（用于父子关系下的并列子项）
- 在父级**完整图号**后添加 `-` 和一位或两位数字（`-1`, `-2`, `-3` …）。
- 结果图号末尾为 `-数字`。
- 适用场景：同一父级下的多个并列子项（如左右对称件、多个垫片）。
**示例**：

父级图号：03S01201-100101  
子零件1： 03S01201-100101-1  
子零件2： 03S01201-100101-2

text

### 2.3 两种方法可以混合使用
同一父级下可以同时存在：
- 通过追加法生成的子组件（纯数字后缀）
- 通过分叉法生成的子零件（`-数字` 后缀）
两者互不冲突，因为后缀形式不同。

### 2.4 字母组件（功能模块代号）
组件层级码不仅可以是纯数字，也可以是**字母或字母数字组合**（如 `ZBC`、`KTC`），
用于表达功能模块（如中部槽组件、机头组件）。字母组件**没有自然的数字递增顺序**，
其下新建组件时使用「根前缀 + 两位数字」的全局编号。

**典型结构示例**：

```text
LS001 (根图号)
└── LS001-00 (主机号，两位数字组件)
    ├── LS001-ZBC (字母组件，功能模块)
    │   ├── LS001-01 (全局两位数字组件)
    │   ├── LS001-02
    │   └── LS001-00-1 (零件，宿主 -00 的序列)
    └── LS001-KTC (字母组件)
        └── LS001-00-2 (零件，与 -ZBC 共享宿主序列)
```

> 说明：图号本身不完整编码层级（`LS001-01` 不以 `LS001-ZBC` 开头），
> 父子关系由模型树的显式结构表达。字母组件下新建**零件**使用**宿主序列**：
> 零件图号取字母组件**父级**的零件序列（`父级-序号`），挂在同一父级下的
> 所有字母组件共享该序列（如 `-ZBC`、`-KTC` 共享 `-00-x`）。

### 2.5 主机层（根的直接子级）
根图号的**直接子级**（层级码为两位纯数字，如 `-00`、`-01`）称为**主机层**。

- 主机层只放**字母组件**（功能模块，如 `-ZBC`、`-KTC`），通过**手动输入**创建；
- 主机层下**不创建零件**，零件应添加到其下的字母组件中。

```text
05S01101 (根图号)
└── 05S01101-00 (主机层)
    ├── 05S01101-ZBC (字母组件)
    └── 05S01101-KTC (字母组件)
```

> 判定规则（程序实现）：`父级 == 根图号` 且父级层级码为**恰好两位纯数字**，
> 即为主机层（如 `-00`）；字母组件（`-ZBC`）与更深层数字组件（`-10`、`-1001`）都不是主机层。
---
## 3. 零件与组件的区分规则
| 类型   | 图号结尾特征                                       | 示例                                       |
| :----- | :------------------------------------------------- | :----------------------------------------- |
| 组件   | **非零件**：纯数字或含字母（如 ZBC）               | `03S01201-10`<br>`03S01201-1001`<br>`05S01101-ZBC` |
| 零件   | **最后一段为短横线 + 数字**，即形如 `-1`, `-2` 等 | `03S01201-100101-1`<br>`03S01201-1001-1`<br>`03S01201-10-1` |
**重要约束**：
- 组件可以有下级子组件或子零件。
- 零件是叶子节点，不可再拥有下级。
- 如果图号以 `-数字` 结尾，则必须视为零件。
- 如果图号不以 `-数字` 结尾且含有层级码（纯数字或含字母），则视为组件。
---
## 4. 新建图号的续号规则（核心算法）
### 4.1 已知条件
- 父级图号（一个有效的组件图号）
- 需要新建的对象类型：**组件** 或 **零件**
### 4.2 通用步骤
1. **确定父级图号**（必须是组件，不是零件）。
2. **扫描当前父级下的所有直接子图号**（仅扫描直接子级，不递归）。
3. 根据新建类型，分别执行续号逻辑。
### 4.3 新建组件（追加法）
- **规则**：新图号 = 父级图号 + 两位数字（`01` ~ `99`），且新图号不能以 `-数字` 结尾。
- **算法**：
  1. 从父级下的所有子组件图号中，提取所有以父级图号开头且**纯数字后缀**的部分。
  2. 找出这些后缀中的最大两位数字（注意：后缀可能不止两位，但只取最后两位）。
  3. 新数字 = 最大数字 + 1（如果不存在任何子组件，则从 `01` 开始）。
  4. 若新数字 > 99，则报错（层级码溢出）。
**示例**：

父级：03S01201-100101  
已有子组件：03S01201-10010101, 03S01201-10010102  
新组件图号：03S01201-10010103

text

### 4.4 新建零件（分叉法）
- **规则**：新图号 = 父级图号 + `-` + 顺序数字（从 `1` 开始递增）。
- **算法**：
  1. 从父级下的所有子零件图号中，提取所有以 `父级图号-` 开头的图号。
  2. 解析末尾的数字 `n`（例如 `-1`, `-2` → 1, 2）。
  3. 取最大 `n_max`，新数字 = `n_max + 1`（如果无子零件，则从 `1` 开始）。
  4. 新图号 = `父级图号 + "-" + 新数字`。
**示例**：

父级：03S01201-100101  
已有子零件：03S01201-100101-1, 03S01201-100101-2  
新零件图号：03S01201-100101-3

text

### 4.5 混合场景下的续号独立性
- 组件续号只看**子组件**（纯数字后缀）。
- 零件续号只看**子零件**（`-数字` 后缀）。
- 两者互不影响，可以独立递增。

### 4.6 字母组件与主机层的续号规则
- 字母组件（层级码含字母，如 `LS001-ZBC`）下新建**组件**：
  使用「根前缀 + 两位数字」的全局编号，即取所有 `前缀-XX`（恰好两位数字）图号的
  最大值 + 1；不存在时从 `01` 开始。深层追加图号（如 `前缀-1001`）不参与计算，
  避免序号冲突。
- 字母组件下新建**零件**：使用**宿主**（字母组件的父级图号）的零件序列，
  即 `宿主-序号`；挂在同一父级下的所有字母组件共享该序列
  （如 `-ZBC`、`-KTC` 共享 `-00-1`、`-00-2`…，序号取宿主下所有零件最大值 + 1）。
- **主机层**（根的直接子级两位数字，如 `-00`）：
  - 新建组件：需**手动输入字母组件**（字母组件无自然递增顺序）；
  - 新建零件：**不允许**，提示将零件添加到其下的字母组件中。
- 数字组件（任意位数的纯数字层级码）下新建组件：仍为追加法，行为不变。
- 数字组件（任意位数的纯数字层级码）下新建零件：分叉法（`父级-序号`）。
- 字母组件本身无法自动续号（字母无自然顺序），需手动输入。

**示例**：

父级：`LS001-ZBC`，已有全局数字图号 `LS001-00`、`LS001-01`、`LS001-10`
（`LS001-1001` 为深层追加图号，忽略）
新组件图号：`LS001-11`

父级：`LS001-ZBC`（挂在 `LS001-00` 下），已有宿主零件 `LS001-00-1`
新零件图号：`LS001-00-2`（与 `LS001-KTC` 等字母组件共享该序列）
---
## 5. 正则表达式识别（供程序解析）
### 5.1 提取固定前缀和层级码
```regex
^([A-Za-z0-9]+)-(.+)$

- 捕获组1：固定前缀
    
- 捕获组2：层级码
    

### 5.2 判断是否为组件（纯数字层级码）

regex

^\d+$

- 若整个层级码完全由数字组成 → 组件
    

### 5.3 判断是否为零件（`-数字` 结尾）

regex

-\d+$

- 若图号末尾匹配该模式 → 零件
    

### 5.4 提取父级图号（用于零件）

将末尾 `-\d+` 去掉即可得到父级图号：

regex

^(.*)-\d+$

---

## 6. 模型树与 BOM 层级的映射建议（供 Creo 二次开发参考）

- 在 Creo 装配中，**每个组件图号对应一个 `.asm` 文件**，每个零件图号对应一个 `.prt` 文件。
    
- 图号的层级关系应通过**子组件嵌套**实现，而非依赖模型树的显示顺序。
    
- 建议在程序层面提供两个核心函数：
    
    - `get_next_component_number(parent_dwg_no)` 返回下一个可用子组件后缀数字
        
    - `get_next_part_number(parent_dwg_no)` 返回下一个可用零件分支数字
        

---

## 7. 完整示例对照表

|父级图号|已有子组件（追加法）|下一个子组件|已有子零件（分叉法）|下一个子零件|
|---|---|---|---|---|
|`03S01201-100101`|`03S01201-10010101`, `03S01201-10010102`|`03S01201-10010103`|`03S01201-100101-1`, `03S01201-100101-2`|`03S01201-100101-3`|

> 注意：子组件和子零件可以同时存在，编号空间独立。

**字母组件与主机层示例**：

|父级图号|新建类型|规则|下一个图号|
|---|---|---|---|
|`LS001-ZBC`（字母组件）|组件|根前缀 + 两位数字（全局，max+1）|`LS001-01`（已有 `-00` 时）|
|`LS001-ZBC`（字母组件）|零件|宿主（`LS001-00`）零件序列|`LS001-00-1`|
|`LS001-00`（主机层）|组件|手动输入字母组件|`LS001-ZBC`|
|`LS001-00`（主机层）|零件|不允许|—|
|`LS001-10`（数字组件）|组件|追加法|`LS001-1001`|
|`LS001-10`（数字组件）|零件|分叉法|`LS001-10-1`|

---

## 8. 边界情况与异常处理

|场景|处理方式|
|---|---|
|父级图号本身是零件（以 `-数字` 结尾）|不允许新建下级，应提示错误|
|父级下无可用的两位数字（达到 99）|报错：组件层级已满|
|父级下零件编号超过约定上限（如无限制）|可继续递增，但需注意图号长度可读性|
|同一父级下混合使用两种规则导致混淆|由算法自动区分后缀形式，不产生冲突|
|主机层（如 `-00`）下创建零件|不允许，提示将零件添加到其下的字母组件|
|主机层（如 `-00`）下自动创建组件|不允许，需手动输入字母组件图号|
|字母组件下自动创建组件|使用根前缀 + 两位数字全局编号，不存在时从 `01` 起|
|字母组件下创建零件|使用宿主（字母组件的父级）零件序列，所有字母组件共享|
